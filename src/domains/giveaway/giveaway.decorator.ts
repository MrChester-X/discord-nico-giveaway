import { createCommandGroupDecorator } from 'necord';

export const GiveawayCommandGroup = createCommandGroupDecorator({
  name: 'giveaway',
  description: 'Раздел команд, связанных с розыграшами',
});
